# dialogue

How much of the text is dialogue, per file and per section. Markdown and plain text use quotation marks; Fountain uses its dialogue blocks.

```sh
bluepencil dialogue
```

Add `--json` for machine-readable output.
