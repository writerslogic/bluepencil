# check

Compares each file against the limits in `[check]` and exits with status 1 if any are exceeded. See [Thresholds](../thresholds.md).

```sh
bluepencil check
```

Add `--json` for machine-readable output.
