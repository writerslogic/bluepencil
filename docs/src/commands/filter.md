# filter

Filter words such as felt, saw, noticed, and realized, which place a character between the reader and the experience.

```sh
bluepencil filter
```

## Options

- `--summary`

Add `--json` for machine-readable output.
