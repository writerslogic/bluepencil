# starters

The most common sentence and paragraph openers, and runs of consecutive sentences that start with the same word.

```sh
bluepencil starters --run 2
```

## Options

- `--run N`

Add `--json` for machine-readable output.
