# echoes

A content word that reappears within a window of words. Names and places are skipped unless `--names` is set.

```sh
bluepencil echoes --window 30 --summary
```

## Options

- `--window N`
- `--min-length N`
- `--names`
- `--summary` group by word

Add `--json` for machine-readable output.
