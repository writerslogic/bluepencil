# freq

Word frequency across all inputs. Common function words are excluded unless you pass `--all`.

```sh
bluepencil freq -n 50 --min-length 4
```

## Options

- `--all` include function words
- `--min-length N`

Add `--json` for machine-readable output.
