# unique

Lexical diversity. MATTR (moving-average type-token ratio) stays comparable across texts of different lengths, unlike raw TTR.

```sh
bluepencil unique --window 100
```

## Options

- `--window N` MATTR window in words

Add `--json` for machine-readable output.
